# Chezmoi ignore file
# These patterns are ignored in the target (home) directory

# README is for the repo, not home
README.md
LICENSE

# OS files
.DS_Store
Thumbs.db

# Local overrides (intentionally not tracked)
.zshrc.local
.aliases.local

{{ if ne .chezmoi.os "darwin" -}}
# Ghostty is macOS-only (no point installing config on headless Linux)
.config/ghostty
.config/ghostty/**
.hushlogin
{{ end -}}
