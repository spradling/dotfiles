# Homebrew Bundle - run with: brew bundle --global
# https://github.com/Homebrew/homebrew-bundle

# ─── CLI Tools ────────────────────────────────────────────────────────────────

brew "bat"              # Better cat
brew "chezmoi"          # Dotfile management
brew "eza"              # Better ls
brew "fd"               # Better find
brew "fzf"              # Fuzzy finder
brew "gh"               # GitHub CLI
brew "git-filter-repo"  # Git history surgery
brew "jq"               # JSON processor
brew "rsync"            # File sync
brew "starship"         # Prompt
brew "timg"             # Terminal image viewer
brew "tree"             # Directory tree
brew "xcodes"           # Xcode version manager
{{ if .personal -}}
brew "speedtest-cli"    # Internet speed test
brew "direnv"           # Per-directory env
brew "nmap"             # Network scanner
brew "node"             # Node.js
brew "ripgrep"          # Better grep
{{ end -}}

# ─── Fonts ────────────────────────────────────────────────────────────────────

cask "font-jetbrains-mono"

# ─── Applications ─────────────────────────────────────────────────────────────

cask "1password-cli"    # 1Password CLI
cask "claude-code"      # Claude Code
cask "firefox"          # Browser
cask "fork"             # Git GUI
cask "ghostty"          # Terminal
cask "sublime-text"     # Editor

{{ if .personal -}}
# Personal apps
cask "discord"          # Chat
cask "dropbox"          # File sync
cask "spotify"          # Music
cask "steam"            # Games
cask "vlc"              # Media player
cask "warp"             # Terminal (backup for IDE features)
cask "caffeine"         # Keep screen awake
{{ end -}}
